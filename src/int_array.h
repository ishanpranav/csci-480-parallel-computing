/** Represents an array of integers. */
typedef int *IntArray;

/**
 * Initializes a new array of integers with the specified `length`.
 * 
 * @param length the size of the array to create.
 * @return A new one-dimensional array of integers with the specified length.
*/
IntArray int_array(int length);
